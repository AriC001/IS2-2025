# Conversion to YAML from Properties formar report
server:
  port: 8080
spring:
  application:
    name: EJ2-b-Rest
  datasource:
    url: jdbc:mysql://localhost:3306/tp2-ej2-db?useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
